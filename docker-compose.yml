version: "2"
services:
  server:
    image : dgoldstein1/websiteanalytics-backend
    environment:
      - LOGGER=true
      - TEST_MODE=true
      - DATABASE_URL=mongodb://sdfsdf:27017
      - PORT=5000
    links:
      - mongodb
    depends_on:
      - mongodb
    ports:
      - '5000:5000'
  mongodb:
    image: 'mongo:4.2.11'
    ports:
      - '27017:27017'